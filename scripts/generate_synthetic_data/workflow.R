source(file.path("scripts", "generate_synthetic_data", "gen_synthetic_abcd.R"))
rm(list = ls())

source(file.path("scripts", "generate_synthetic_data", "gen_synthetic_financial_data.R"))
rm(list = ls())

# source(file.path("scripts", "generate_synthetic_data", "gen_synthetic_crispy.R"))
# rm(list = ls())

# source(file.path("scripts", "generate_synthetic_data", "generate_synthetic_portfolio_data.R"))
# rm(list = ls())
